services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: loopbox
      POSTGRES_PASSWORD: loopbox
      POSTGRES_DB: loopbox
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U loopbox"]
      interval: 2s
      timeout: 2s
      retries: 20

volumes:
  pgdata:
