# Scraped competitor research — do not commit
docs/competitor-analyze/
competitor-analyze/

# Scratch space for trying out third-party tools/frameworks — never commit
# Match only root /sandbox; an unanchored pattern would also hide packages/sandbox.
/sandbox/

node_modules/
dist/
.turbo/
coverage/
.env
*.log
.dev.vars

# Local tooling state — not project source
.playwright-mcp/
.claude/
.agents/
skills-lock.json
.vercel/
